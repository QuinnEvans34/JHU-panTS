# ---- Raw data — NEVER commit (hard constraint; lives on the external drive) ----
data/
*.nii
*.nii.gz
*.tar.gz
*.xlsx

# ---- Pretrained weights & checkpoints (large) ----
pretrained_weights/
*.pth
*.pt
*.ckpt

# ---- Outputs: predictions, figures, checkpoints, logs, peek images ----
outputs/

# ---- Experiment tracking ----
mlruns/
mlflow.db
*.mlflow

# ---- Python virtual environments (this was the 30k-file problem) ----
# .venv*/ catches .venv, .venv312, and any other venv folder
.venv/
.venv*/
venv/
env/
ENV/

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/

# ---- Node / React UI (Week 5) ----
node_modules/
dist/
build/
.next/

# ---- Presentation render byproducts (slide/QA image exports, office temp & lock files) ----
# Keep: the .pptx/.pdf deck, week3/diagrams/**, and week3/mlflow-*.png screenshots.
# Ignore: the per-slide .jpg exports and QA thumbnails at the week3/ root, plus LibreOffice temp/lock files.
week3/*.jpg
*.tmp
.~lock.*#

# ---- Env & OS ----
.env
.DS_Store
