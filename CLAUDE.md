# CLAUDE.md — Agent Context

Context file so any AI session picks up this project correctly. Keep updated as things change.

## Project
3D pancreas-aware pancreatic lesion segmentation on the JHU **PanTS** dataset (MONAI/PyTorch). Primary target: **Level 4.5** (background / pancreas / lesion). Config-driven so Level 4 (lesion-only) and Level 5 (multi-structure) share one codebase. **Segmentation tool, not a diagnostic system** — no clinical claims.

## Owner / scope
Solo project (Quinn). Course: 5-week independent ML project, proposal → delivery (instructor-approved). Business framing: radiologist / imaging-annotator annotation-assist (accept/edit/reject proposed contours). **Capstone:** a follow-on 10-week project will extend this same codebase (ROI cascade, full-scale training, Level 5) — design for that now.

## Hardware & environment (important)
- **14" MacBook Pro, Apple M5 Pro** — 18-core CPU, **20-core GPU**, 16-core Neural Engine.
- **64 GB unified memory** (shared CPU/GPU — no separate VRAM limit; generous for 3D patches).
- **1 TB SSD** internal; dataset lives on an **external drive** (~340 GB), NOT on the laptop or in the repo.
- **Apple Silicon → PyTorch MPS backend, NOT CUDA.** Implications for tomorrow:
  - Use `device = "mps"`; some MONAI/PyTorch ops fall back to CPU on MPS (set `PYTORCH_ENABLE_MPS_FALLBACK=1`).
  - Mixed precision (`autocast`) support on MPS is partial — validate before relying on it; may keep fp32.
  - `nnU-Net` / heavy CUDA-only paths won't apply; stick to MONAI 3D U-Net / SegResNet.
  - Patch size is bounded by unified memory + MPS stability, not a fixed VRAM number — start moderate (e.g. 96×128×128) and tune.

## Hard constraints / guardrails
Never commit raw data · split by patient not slice · full-volume sliding-window inference for eval (not patch-only) · report pancreas & lesion Dice separately · tumor-positive patch sampling required · no clinical/diagnostic claims · don't start Level 5 before 4.5 works · keep pipeline config-driven · dataset on external drive, path via config not hardcoded.

## Key technical decisions (locked, 2026-06-30)
- **Scope:** segmentation (Level 4.5) + a derived **CADe detection wrapper** = "there could be a tumor here." No tumor-type call, no diagnosis. Tumor-type classification is a research-only stretch if core finishes early. Full design in `docs/architecture.md`.
- **Model:** SegResNet (MONAI) primary; Swin UNETR as optional later comparison.
- **Transfer model (LOCKED, 2nd-opinion verified):** fine-tune **SuPreM's SegResNet** (`supervised_suprem_segresnet_2100.pth`, ~4.7M params, AbdomenAtlas pretraining, same JHU lab). From-scratch SegResNet = baseline. **Two-track rule:** clean ablation uses plain checkpoint-compatible `SegResNet` (NOT `SegResNetDS`, no deep supervision/norm swap — would break weight loading + confound the comparison); a separate "Track B" practical model is free to add deep supervision etc. Fallback if it stalls = SuPreM U-Net, not Swin/nnU-Net. PanTS checkpoints (MedFormer/R-Super) = reference oracle only.
- **2nd-opinion refinements (adopted):** match SuPreM's input normalization; test wider HU window vs `[-100,300]`; harder negatives near pancreas; add NSD + per-case CC lesion sensitivity, `ignore_empty`; stitch sliding-window output on CPU (MPS memory); treat SuPreM repo as checkpoint-source-only (old deps). **Anatomy-context ablation** (add a few neighbor structures) is high-value: PanTS shows +10pp tumor Dice from richer anatomy labels.
- **Metrics:** report pancreas & lesion Dice separately; headline = patient-wise sensitivity + specificity (CADe). SOTA reference: tumor Dice ~0.53, P-sens ~80%, spec ~90% → lesion Dice ~0.35–0.50 is a respectable target.
- **Training recipe (LOCKED, 2026-07-01):** Track A starting recipe in `docs/training.md` §0 — SegResNet (init_filters=16, GroupNorm, match SuPreM), spacing 1.5³, HU [-100,300]→[0,1], ROI 96³, RandCropByPosNegLabel num_samples=4 pos:neg 2:1, DiceCELoss, AdamW wd 1e-5, LR 2e-4 scratch / 1e-4 transfer (freeze encoder 2-3 ep), warmup 2ep→cosine, fp32 on MPS, seed 42, 1 epoch=250 iters, val every 5 ep, best-by-lesion-Dice. Stage 0 overfit gate → Stage 1 pancreas → Stage 2 L4.5 → Stage 3 lesion-focus → Stage 4 sliding-window eval. Confirm SuPreM's exact config/spacing/normalization at code time.
- **UI (LOCKED, 2026-07-01):** **React + NiiVue** (WebGL NIfTI viewer) + Tailwind/shadcn. **Static-first:** pipeline pre-computes predictions (NIfTI + `results.json`); UI reads saved files, no backend for the demo. Live FastAPI inference = capstone stretch. Build in **Week 5** after the model works; `scripts/peek_case.py` PNGs are the fallback. Design in `docs/ui.md`.

## Design docs (the plan)
- `docs/architecture.md` — master design doc + index (scope ladder, 3D handling, model, transfer learning, metrics, capstone scope).
- `docs/training.md` — model/norm/deep-supervision, loss, AdamW+schedule, sampling, ROI cascade, training-time, checkpoint/resume.
- `docs/experiment-tracking.md` — MLflow (required): local setup, what to log, scratch-vs-transfer comparison.
- `docs/ui.md` — React + NiiVue clinical review experience: CADe summary + 3D mesh (marching cubes) + tri-planar viewer + mask export.
- `docs/data-pipeline.md` — **finalized against real files** (on-disk layout, manifest schema, label remapping per level, splits, dev subset).

## Current state (as of 2026-06-30, Week 1)
**Done:** Repo + docs scaffolded. Dataset verified incl. on-disk layout (sharded image tarballs + per-case label dirs + `metadata.xlsx`; partial download = labels + one ~34 GB shard). Proposal/schedule/pitch/logs drafted. **Full design captured** across architecture/training/experiment-tracking/ui docs. Decisions added: AdamW + cosine/warmup + differential LR; InstanceNorm + deep supervision; whole-body → single-stage ROI now / cascade for capstone; MLflow primary tracker; UI = slices + 3D mesh.

**Data acquisition COMPLETE (2026-07-01):** full PanTS Mini on the external drive at `/Volumes/JHU-PanTS/PanTS/data/` — **9,000 train images** (`ImageTr/`), **901 test images** (`ImageTe/`), all ~9,901 label sets (`LabelAll/<id>/segmentations/` + `combined_labels.nii.gz`), `metadata.xlsx`. 382 GiB used, 83 GiB free. Layout + manifest design finalized in `docs/data-pipeline.md`. (Gotcha for next time: macOS BSD `tar` rejects the scripts' `--checkpoint` flag → extract with plain `tar -xzf`; keep drive connected + lid open during long ops.)

**Pipeline code (2026-07-01):** `src/utils/` (config/seed/paths) + `scripts/build_manifest.py` + `scripts/create_splits.py` written, tested on synthetic, and RUN on real data. Real facts locked: **no patient id in metadata → each scan = its own case/patient** (case==patient RESOLVED); metadata join key = `PanTS ID`; **tumor prevalence 10.4%** (1,033/9,901); lesion volume 2–732,388 mm³ (median ~4,700); splits = **7,200 train / 1,800 val (both 9.8% tumor) / 901 official test (16.8%)** + dev subset 100 @ 50%. Manifest → `outputs/manifest.csv` (29 cols); splits → `outputs/splits/`.

**Data pipeline VALIDATED end-to-end (2026-07-01):** `src/data/transforms.py` + `dataset.py` written; `sanity_check_case.py` RAN on real case PanTS_00001070 and PASSED (**Week 1 milestone hit**) — resampled 190×134×131 @1.5mm, intensities [0,1], label {0=bg,1=panc,2=lesion} with lesion ~0.04% of volume, 96³ patches with 3/4 containing lesion (pos sampling works), overlays correct. Data pipeline (manifest→splits→transforms→patches) DONE.

**Model + training DONE + STAGE 0 VALIDATED (2026-07-03):** `src/models/segresnet.py` (SuPreM config matched: init_filters=16, GroupNorm num_groups=8, blocks (1,2,2,4)/(1,1,1); loader strips `module.`, unwraps `net`, re-inits 32→3 head), `src/training/` (losses/metrics/trainer), `scripts/train.py`, `scripts/inspect_checkpoint.py`. Overfit run: **pancreas Dice 0→0.888, loss 1.87→0.54 — pipeline proven end-to-end**, MLflow logging. **Env moved to Python 3.12 venv (`.venv312`)** — 3.14 broke mlflow install; 3.12 fixes it. **MPS ~0.29 it/s (~3.4s/step) — slow; course stays on dev subset.** **Lesion Dice stuck at 0** (0.04% class ignored under the loss) → set `loss.include_background: false`; next lever DiceFocal. Fixes en route: GroupNorm needs num_groups tuple; SpatialPadd (some scans <96 deep); CacheDataset for speed; flat LR + pancreas-based gate for overfit.

**STAGE 0 PASSED + LESION LEARNS (2026-07-03):** with `loss.include_background: false` + `--positive` overfit (2 tumor cases), **lesion Dice climbed 0→~0.7** and pancreas learned too — the model can fit both classes. Key learning: earlier lesion=0.000 was because the first dev_subset cases were tumor-FREE (metric NaN printed as 0); `train.py` now prints `n/a` vs real 0, and `--positive` draws overfit cases from tumor-positive only. Dynamics: lesion learns later than pancreas; classes compete (include_bg=False); train-crop Dice is noisy. **Validation added:** `src/inference/sliding_window.py` (`predict_volume` + `validate`), wired into `train.py` (`--val-limit/--val-every`, best.pt by val lesion Dice).

**First Stage 2 run DONE + KEY EVAL FINDING (2026-07-04):** SuPreM transfer on dev_subset (100), 6000 iters. Training-time val lesion read 0.000 — but that was a **measurement artifact** (val set ~90% tumor-free; lesion Dice on tumor-free scans is NaN/0). Built `scripts/evaluate.py` to score properly: on tumor-POSITIVE val cases **pancreas Dice 0.716, lesion Dice 0.174** (real, non-zero — model DOES find tumors). BUT **specificity only 8%** (1/12 healthy scans not flagged) → model is **trigger-happy / over-predicts lesion**. Classic sens/spec tradeoff: DiceCE+bg was too conservative, DiceFocal+no-bg+67%-pos too aggressive. **Fixes applied:** lowered sampling to pos:neg 1:1 (~50%); added `--val-positive` (validate on tumor-positive cases). **Next levers:** re-run + evaluate; inference post-proc (largest-CC + volume threshold) to prune false positives; ultimately more data (capstone) for both sens & spec. MPS ~0.30 it/s.

**WEEK 2 IN PROGRESS (2026-07-06):** EDA + report deliverables built from real manifest. `deliverables/week2/eda-notebook.ipynb` (9 sections, 5 real-data viz + overfit-curve pulled from mlflow.db) and `deliverables/week2/data-understanding-report.md` (M2A1 sections 1-5) done. `docs/implementation-plan.md` created; `docs/AI-usage.md` renamed to `docs/ai-usage-log.md` (M2A1 rubric name) + Week 2 entry; Monday standup added. Airflow explicitly ruled out in report (static dataset, no recurring ingestion → scripts give same reproducibility).

**KEY EVAL FINDING (2026-07-06, `last.pt` step 6000, val split, n=12/12):** pancreas Dice **0.720**, lesion Dice raw **0.169** / cleaned **0.139**, specificity **8%** (1/12) raw AND cleaned. Interpretation: model finds tumors but badly OVER-PREDICTS; CADe post-proc does NOT help (spec unchanged, lesion Dice dropped) → false positives are LARGE CONNECTED regions, not prunable specks. This checkpoint used the aggressive recipe (DiceFocal/no-bg/heavy pos sampling); config now rebalanced to 1:1 but NOT yet retrained. **Sens/spec strategy (implementation-plan.md):** (1) retrain balanced 1:1 + re-eval, (2) lesion probability-threshold sweep, (3) constrain lesion ⊂ predicted pancreas (anatomical), (4) harder negatives near pancreas, (5) more data = capstone.

**SENS/SPEC LEVERS CODED (2026-07-06, unit-tested, awaiting balanced retrain):** (2) lesion probability-threshold sweep → `evaluate.py --sweep --thresholds ...` (monotonic tradeoff verified); (3) anatomical constraint `lesion ⊂ pancreas` in `postprocess.py` (`constrain_lesion_to_pancreas`, dilate pancreas by margin_mm, demote floating lesion CCs to bg; guarded when no pancreas predicted) → `evaluate.py --lesion-within-pancreas-mm 10`; (4) harder-negatives sampling via `RandCropByLabelClassesd` → `sampling.strategy: classes` + `class_ratios [1,1,2]` in config (default stays `posneg`, backward compatible). All three compile + logic tested on synthetic (no torch in sandbox). NOT yet run on real checkpoint — need balanced retrain first.

**LEVERS EVALUATED ON REAL MODEL (2026-07-06 eve, aggressive `best.pt` step 6000, val n=12/12):** anatomical constraint is the STRONG lever — specificity **8%→42%** (1/12→5/12) via `--lesion-within-pancreas-mm 10`, no retrain. Threshold sweep is WEAK here (lesion Dice flat ~0.16-0.18, spec stuck 8% until thr 0.90→25%) → far-field FPs are high-confidence + near organ, so geometry removes them but a prob cutoff can't. Cost: cleaned lesion Dice 0.169→0.139 (expected sens/spec trade). Narrative UPGRADE: old "post-proc didn't help" replaced with "size-cleanup failed, geometric constraint worked" across report §4.3, presentation Finding 3 + Q&A, implementation-plan. **Balanced retrain launched overnight 2026-07-06** (train.py dev_subset, 1:1 sampling, 6000 iters, --val-positive) — eval `best.pt` in AM, compare vs this baseline; hope = balanced base + constraint stack. Refinement to test: constraint + relax largest-CC (true lesions are inside pancreas) may recover lost lesion Dice.

**BALANCED RETRAIN = NULL RESULT (2026-07-07, balanced `best.pt` step 6000, val n=12/12):** balanced 1:1 sampling came out ~IDENTICAL to aggressive — pancreas 0.721 vs 0.720, lesion 0.169 vs 0.169, spec raw 8% vs 8%, spec cleaned 42% vs 42%, threshold sweep near-identical (spec 8% until 0.80→17%/0.90→25%). Sampling ratio is NOT the cause of over-prediction → RULED OUT. Anatomical constraint reconfirmed as the one robust lever (8→42% on both models). **Revised lever order: (1) LOSS is now top model-side lever** — current DiceFocal `include_background:false` never rewards correct background → model has no reason to NOT paint lesion. Tonight's experiment: flip `loss.include_background: true` (single variable vs balanced run; focal term should protect lesion from being buried). Fallback knobs wired in build_loss: lambda_dice/lambda_focal/gamma/class-weights. (2) keep constraint at eval, (3) harder-negatives `strategy:classes` only if loss insufficient, (4) more data = capstone. (Drive dropped overnight → FileNotFound on eval; remount fixed it. Consider preflight check.)

**EXP FRAMEWORK + CONTEXT EXPERIMENT (2026-07-07):** `docs/experiments.md` now the formal hypothesis log (EXP-01 loss=tonight, EXP-02 patch/field-of-view). Wired CLI overrides so field-of-view is a one-command test, NO config edits: `train.py --patch 128 --num-samples 4` (overrides sampling.patch_size + inference.sw_roi_size together) and `evaluate.py --roi 128` (must match trained patch). Defaults None = no-op (tonight's loss run unaffected). Run name auto-includes patch (`..._p128_...`). EXP-02 hypothesis: 128³ (19.2cm FOV, whole pancreas) vs 96³ (14.4cm); accept if pancreas Dice +0.03 OR lesion +0.02 at n=20/20; queued behind loss exp; ~8-11h at num_samples 4 (drop to 2 if MPS OOM). Patch is transfer-safe; widening init_filters is NOT (breaks SuPreM load) → capstone.

**EXP-07 LOSS bg1 = REJECTED (2026-07-08, resumed to 6000 after a macOS update killed it at step 5000):** include_background:true did NOT move raw specificity (still 8%/1-of-12), and it HURT the deployable numbers — lesion Dice 0.169→0.149, cleaned spec 42%→33%. Secondary effect kept for the record: bg1 recalibrated confidence so the threshold sweep finally works (spec 33% @0.80, 42% @0.90 vs bg0's 25% @0.90), but that ties bg0+constraint without beating it and costs Dice. **Reverted config to include_background:false (bg0 = best base).** TWO training knobs now (sampling EXP-05 + loss-bg EXP-07) failed to move raw specificity → over-prediction is a DATA-SCALE problem, not a training-knob problem. Strategy pivot: stop chasing raw specificity with loss/sampling; bank post-proc (constraint=42%) + more data at capstone. Next = EXP-08 patch/field-of-view (tests ACCURACY, a different axis), run on bg0.

**EXP-10 ORACLE ROI = BEST LESION DICE YET (2026-07-09, cropped-ROI eval n=20/20):** crop to GT pancreas + 1.0mm resolution → pancreas 0.726, lesion RAW **0.234** / cleaned 0.214, vs 96 baseline lesion 0.206 → **+0.028, highest lesion Dice so far.** Modest but real, short of the +0.05 bar. TWO caveats: (1) ORACLE (uses GT pancreas mask = "with provided ROI" upper bound, not autonomous); (2) this run got the encoder-training bug fix baseline lacked, so part of gain may be encoder, not resolution. Next: EXP-10b = re-run 96 baseline with fixed code to isolate; then SCALE UP tumor cases (main lever). Cascade adopted as capstone direction. build_optimizer now includes ALL params (fixes resume + the latent frozen-encoder bug); transforms.py uses RandSpatialCropSamplesd when crop_to_pancreas set; --spacing/--crop-pancreas CLI on train+eval.

**EXP-08 PATCH 128 = REJECTED ON ACCURACY (2026-07-09, matched n=20/20):** 96 base vs 128 → pancreas 0.740 vs 0.747 (+0.007, fails +0.03), lesion RAW 0.206 vs 0.187 (128 WORSE), lesion cleaned 0.197 vs 0.175. The overnight "win" was an artifact of comparing 128@n20 vs old 96@n12; re-scoring 96 at n20 gave 0.206 and erased it (→ matched baseline is mandatory). 128 DID raise specificity (raw 10%→25%, cleaned 35%→40%) = a sens/spec trade, not accuracy. **Best accuracy base stays plain 96 bg0 (lesion 0.206 = highest yet).** Context now ruled out for accuracy → isolates RESOLUTION as the untested lever → **EXP-10 (crop to pancreas + finer 1.0mm) is the key next run; baseline to beat = lesion 0.206.** EXP-10 code wired (`--crop-pancreas`, `--spacing` on train+eval; transforms crop to GT pancreas label). Smoke-test first (50 iters), then overnight.

**EXP-11 CLARITY = INCONCLUSIVE + EVAL BUG CAUGHT (2026-07-09/10):** crop-native + 0.7mm + 64 patch + random sub-patch. Full eval printed pancreas 0.217 / lesion 0.024 — looked catastrophic but was a MEASUREMENT BUG: `evaluate.py` defined `--crop-native` but never applied it to cfg, so eval ran the WHOLE BODY through a model trained on tiny cropped ROIs (total train/eval mismatch; pancreas collapse to 0.217 was the tell). Fixed (evaluate.py now applies `--crop-native`). Trustworthy in-loop val was 0.661/0.100 — still below baseline because 4 things changed at once and 2 starved the model: 64 patch @0.7mm = only 45mm FOV (thin slab of a 150-200mm organ) + random (non-tumor-biased) sub-patches rarely centered the tumor. REJECTED as run; motivated EXP-12.

**EXP-12 WHOLE-BOX = NEW BEST, WINS ON ALL 3 AXES (2026-07-10, eval n=20/20):** Quinn's idea — instead of random sub-patches inside the pancreas crop, feed the ENTIRE pancreas box as one fixed cube every step (crop-native bbox+16vox → resample 1.5mm → `ResizeWithPadOrCropd` to 128³, no random sampler; 128@1.5mm spans 192mm so whole organ fits). SuPreM transfer, bg0, dev_subset_clean (95 cases), 6000 iters, val on 20 tumor-pos. **Results: pancreas 0.807 (from ~0.72), lesion RAW 0.263 / cleaned 0.252 (BEATS the 0.234 bar = best to date), specificity RAW 11/20=55% (vs 8% on EVERY prior model).** Mechanism is clean: raw==cleaned spec + near-flat cleaned lesion Dice → model now stays quiet on healthy scans by itself, so the anatomical constraint (old strong lever) is a near no-op. Sweep = usable CADe dial (thr 0.70 → les 0.253/spec 60%; 0.90 → 0.234/70%). **New Track-A best base.** CAVEATS for writeup: (1) ORACLE ROI ("radiologist provides box"), not autonomous; (2) spec leap partly STRUCTURAL (less tissue inside box to false-alarm on) → 8%→55% not like-for-like vs whole-scan; (3) composite change vs 0.234 (spacing+crop+FOV+sampling all moved), n=20 (each free case=5pp) → evidence "whole-box at feasible resolution works," not proof one knob did it; (4) shares encoder-fix (controlled vs EXP-10). Code: `whole_box` mode in transforms.py (`ResizeWithPadOrCropd`, applies train+val), `--whole-box` on train+eval, `--roi` now sets sampling.patch_size too (was the same bug class as EXP-11), `PadEmptyCropd` guard (only pads degenerate crops, no native-space FOV drift), resume-unfreezes-encoder fix. All Codex-audited (design review: RUN AS PLANNED + val=20). Run executed in `.venv` (no mlflow) → re-log via `scripts/log_run_to_mlflow.py` (built + verified).

**EXP-13 CLARITY CURRICULUM = SOFT NULL + CONFOUNDED SPEC SURPRISE (2026-07-11, prof's idea, n=20/20):** train weighted toward sharpest scans (clarity20, median 0.80mm) vs representative (repr20, 1.50mm), tumor count held constant. Accuracy soft null (lesion 0.204 vs 0.189, under +0.02 bar; pancreas flat) — resampling to 1.5mm normalizes native resolution away. Surprise: specificity 50→85% raw / 60→90% cleaned, but CONFOUNDED (sharp cohort also mostly non-contrast). Hypothesis-generating, not causal → motivated EXP-14.

**EXP-14 CONTRAST PHASE = DECISIVE, STRONGEST EXPLANATORY RESULT (2026-07-12, n=20/20):** isolated contrast at matched resolution — nc20 (non-contrast) vs pv20 (portal-venous). Non-contrast trains a FAR more specific model: raw spec 90% vs 10%, cleaned 95% vs 10% (16-case gap). Confirms contrast phase, NOT resolution, drove the EXP-13 surprise. Mechanism: subtle non-contrast tumors → model learns caution; conspicuous PV tumors → model over-fires (18/20 healthy flagged). Lesion Dice ~tied (size-confounded, pv tumors 1.9x larger). Product implication: contrast phase = operating-point dial (non-contrast→specificity/screening, PV→sensitivity). Headline for Week 3 check-in.

**WEEK 2 SUBMITTED (2026-07-12):** M2A1 + M2P2 + M2A2 finalized. Report/notebook: added explicit "three most important findings" callout (imbalance, geometry, over-prediction); corrected acquisition metadata everywhere (four real makers Siemens/GE/Philips/Toshiba from 6 raw strings — GE+Philips double-counted; site field is grouped labels + codes, many more than 20 institutions), incl. the notebook's live count cell + cached output. Report technical depth kept for M2A1. Still TODO: re-log EXP-12/13/14 to MLflow via `scripts/log_run_to_mlflow.py` in `.venv312` (check UI first to avoid dupes).

**WEEK 3 KICKOFF (2026-07-12):** model-experimentation phase. Code audited end-to-end — clean; EXP-11-class train/eval mismatch, encoder-training, and resume bugs all confirmed fixed. Queue chosen for the Friday check-in (over raw model gains): (1) EXP-09 transfer-vs-scratch on the whole-box recipe (defends SuPreM choice at 1-on-1; fire-and-forget overnight; single var --scratch vs --transfer; LR/freeze confound noted); (2) EXP-15 = 8-view flip TTA at eval, now wired (`evaluate.py --tta`, `predict_probs_tta` in sliding_window.py), run on the EXP-12 whole-box ckpt; (3) code cleanup done — annotated not-yet-wired config keys (`training.cache`, `validation.interval_epochs/patience`) + flipped `build_loss` include_background default to False (bg0 locked base). DEFERRED: disk/persistent cache for data scale-up (biggest lever, heaviest code, longest runs; top item after the check-in, real Week 4/capstone push). Both EXP-09 and EXP-15 entries written in experiments.md with pre-registered bars.

**CHECKPOINT HYGIENE = CONFIRMED LOST + FIXED IN CODE (2026-07-12):** EXP-12 whole-box best (lesion 0.263) is confirmed GONE (2nd Claude session searched whole tree; current `best.pt`/`last.pt` = EXP-14 pv20; never an MLflow artifact since EXP-12 ran in `.venv`). Only Time Machine (snapshot < Jul 11 13:18) could recover exact bytes; otherwise REGENERATE via the EXP-12 command (seed 42 reproduces within n=20 noise) — folds into the EXP-09 transfer arm tonight. **Root cause fixed in `train.py`:** every run now auto-archives keepers to `outputs/checkpoints/pants-level45/runs/<run_name>__<timestamp>/` (best.pt + last.pt + run_info.txt), appends a row to `run_ledger.csv`, and prints a LOUD banner if MLflow is unavailable — all independent of MLflow. Manual `cp` no longer required; point eval at the archived path for keepers. Full note in `docs/experiments.md` (Checkpoint & logging discipline).

**EXP-09 + EXP-12 REGEN DONE (2026-07-14, eval n=20/20):** EXP-12 whole-box REGENERATED faithfully after the loss — transfer step-2000 best = pancreas 0.778, lesion 0.257 raw / **0.263 cleaned**, specificity 50% raw / **55% cleaned** (reproduced original 0.807/0.263/55% within noise; 55% spec was NOT a lucky draw). Pinned as `wholebox_p128_1p5_GOOD.pt` (archive `transfer_wholebox_p128_1p5__20260713_221119`). **EXP-09 transfer-vs-scratch = ACCEPT DECISIVELY:** scratch step-4000 = pancreas 0.650, lesion 0.120, spec 0% → transfer wins +0.13 lesion / +0.13 pancreas / +50pp spec, far past the 0.03 bar. SuPreM pretraining is what makes the model work at 95 cases; defends the model choice for Friday. Confound noted (arms differ in LR + freeze). Recovery lessons logged in experiments.md: `--resume` corrupts keepers (resets best_val, clobbers best.pt) → runs must be uninterrupted until resume is hardened; specificity is the noisy/run-variable metric, lesion Dice reproduces tightly (0.236/0.257/0.263). The per-run archive is what saved us. Report rows filled (`deliverables/week3/ml-experimentation-report.md`).

**EXP-15 TTA = SPECIFICITY DIAL, NOT FREE (2026-07-14):** 8-view flip TTA on EXP-12 → lesion 0.263→0.238, specificity 55→80% cleaned. Rejected as default (trades lesion for spec) but kept as an operating-point lever (reaches 80-90% spec regime the threshold sweep can't). Logged.

**EXP-16 RESOLUTION = REJECTED (2026-07-15):** finer whole-box (160@1.2mm, matched FOV) → pancreas 0.778→0.805 but lesion 0.257→0.248 raw (null on target). Fourth recipe axis ruled out for lesion accuracy (after sampling/loss/context) → isolated data as the only lever.

**EXP-17 DATA SCALE-UP = BREAKTHROUGH, NEW BEST (2026-07-16):** held whole-box recipe fixed, tripled tumor cases (dev_subset_clean 95/50 → scaled300 300/150). **Lesion Dice 0.263→0.313 raw / 0.327 cleaned (+0.05/+0.064), pancreas 0.778→0.815, specificity 50% (held).** Cleared the 0.30 bar — FIRST result all week to beat 0.263. Post-proc now HELPS (cleaned>raw); sweep strictly better (thr 0.90 → lesion 0.319 / spec 70%). **Confirms the central hypothesis: lesion accuracy is DATA-limited, not recipe-limited. Data scale is THE lever.** Split built by `scripts/make_scaled_split.py`. Ran in `.venv` (py3.14, no MLflow) → re-log. Pinned `wholebox_scaled300_GOOD.pt` (archive `transfer_wholebox_scaled300__20260715_205005`). New candidate model for the report.

**METRICS AUDIT = CORE CORRECT, ONE REAL FINDING (2026-07-17, Claude + Codex, `docs/codex-metrics-audit.md`):** Two independent audits confirm the Dice/specificity arithmetic is correct, NO train/val leakage (7200/1800 IDs, 0 overlap), healthy cases correctly excluded from lesion Dice via `ignore_empty` (the reason no 1.0s ever appear — that's the correct signature, not a bug). **The one real finding (Moderate): the "oracle pancreas box" is built from pancreas UNION lesion (`_foreground_label`=label>0), so lesion extent leaks into the ROI → lesion Dice is an UPPER BOUND.** Relative conclusions all hold (every run used the same crop); absolute number is optimistic. FIXED behind `preprocessing.roi_source` (union=legacy default | pancreas=organ only), wired `--roi-source` on train/eval, own cache tag, ComposeLabeld keeps `panc_roi`. **EXP-19 pre-registered** = pancreas-only control on scaled300 to quantify the leak (smoke-test first — new crop path). Other findings all Minor/framing: eval `dice()` now returns NaN on empty GT + `nanmean` (FIXED, was latent 1.0-inflation risk, not triggering); relabeled spec@50mm3 / mask-negative / development-validation; analyze_cases full-precision. Report §4 caveats + implementation-plan updated. Queued: embed config in ckpt; full-cohort + CI eval; final held-out test pass. Do NOT change roi_source default (would break comparability + the running scaledmax cache).

## FINAL STATUS (Week 5, 2026-07-28) — read this first

**Course project COMPLETE.** Weeks 1–4 submitted; Week 5 (M5P1 demo + M5A1 package + M5A2 standups) in flight.

**THE headline number (use this one, everywhere):** official held-out TEST set, 901 cases (151 tumor-positive + 750 tumor-free), scored ONCE with the registered model — **lesion Dice 0.474 raw / 0.472 cleaned [95% CI 0.42–0.52], pancreas Dice 0.827, detection sensitivity 96% (145/151), specificity 17% (128/750)**. Reference: PanTS leaderboard ~0.53 (MedFormer 52.9 / R-Super 53.4). **This is PROVIDED-ROI (oracle pancreas box) — always say so.** ⚠️ **0.528 and 0.483 are CONTAMINATED (pre-leakage-fix) — never cite them.** 0.415 = clean *validation* number (n=40).

**Registered model:** MLflow `pancreas-lesion-segmenter` **v1**, step 18000, sha `62cc72fd…`, archive `runs/transfer_wholebox_scaledmax_CLEAN__20260719_012104/best.pt`. Recipe: SegResNet + SuPreM transfer, whole-box (crop-native 16 → 128³ @1.5mm), DiceFocal bg0, `roi_source=union`.

**Full-suite benchmark row (all 901, 2026-07-27):** pancreas Dice (ALL cases) **0.847**, lesion Dice positives 0.474, whole-cohort lesion Dice 0.221 (empty=1.0) / 0.093 (empty excl), **patient-level AUC 0.804**, mean FP volume 1830 mm³ (622 flagged negatives). The AUC is the key capstone insight: the model *can* discriminate; 17% specificity is a badly-placed threshold, not an inability.

**Capstone levers, in evidence order:** (1) **volume gate** in front of the segmenter — free, no retrain, lifts specificity 17%→43% at 90% detection (AUC on volume = 0.843); (2) **dedicated tumor-presence classifier** as a better gate (the real centerpiece); (3) **autonomous localize→segment cascade** to remove the oracle ROI; (4) **scale to all 9,000 cases**. **RULED OUT:** retraining the segmenter for specificity — EXP-25 (1:9) and the 1:3 run both proved gating the good segmenter dominates a timid one at every matched operating point (1:3 = spec 53%/det 86%/lesion 0.426, still beaten by baseline+gate).

**Live system:** `scripts/serve.py` FastAPI (`/health`, `/cases`, `/predict`, ~0.6s/scan) serving from `ui/public/cases/`; React + NiiVue UI (`ui/`, `npm run dev`) with live Analyze, unmarked-scan → predict → reveal-truth flow, and 3D marching-cubes meshes. UI falls back to cached results when the endpoint is offline.

**Week 5 deliverables:** `deliverables/week5/` (ui-walkthrough, how-to-use, retrospective, ui-screenshots) + finalized README / ai-usage-log / implementation-plan / this file.

## How AI was actually used on this project (final, vs. how it was planned)

Planned in Week 1: AI as a coding accelerator and explainer. What actually happened was broader and more adversarial.

- **Research / decision stress-testing** (Week 1) — ChatGPT deep research independently validated the SuPreM SegResNet choice *before* it was locked in.
- **Explainer** (Weeks 2–3) — the highest-leverage and least visible use: 2D→3D concepts, Dice, class imbalance, voxels. Compressed weeks of reading into days.
- **Adversarial review became the core workflow** (Weeks 3–5) — the loop for anything non-trivial: write a spec → second AI reviews the *plan* → revise → write code → review the *code* → require a regression test per issue found. This caught the validation-leakage bug, three rounds of real deployment bugs, a train/serve double-processing bug, and a 3D viewer loading the wrong data.
- **Parallel specialists** (Week 5) — Codex on the React/NiiVue UI, Claude on analysis + documentation, each checking the other.
- **The consistent failure mode:** AI is confident about data it has not inspected. Every real error was an assumption about data shape, not a syntax mistake. Standing rule: make it read the file / run the command / show the output before it asserts anything about this repo.
- **What stayed human:** the whole-box idea, the decision to train EXP-26 to convergence instead of reporting a flattering intermediate, and every accept/reject call against a pre-registered bar.

Full narrative in `docs/ai-usage-log.md` (final retrospective section).

## Prior session notes (historical)
Week 3 SUBMITTED (M3P1 report/deck/audience notes + M3A2 standup log, both committed). Headline at the time: EXP-17c whole-box scaledmax — lesion 0.528 provided-ROI (**later found contaminated by validation leakage — superseded by 0.474 above**). Data scale-up (EXP-17→17c) proved data is the lever.

**EXP-26 ANATOMY-AWARE = REJECTED at convergence (2026-07-22).** Full clean single-variable experiment (5-class bg/head/body/tail/lesion, collapsed metrics, frozen hashed cohorts, shared init, deterministic resume; docs/spec-exp26-anatomy-aware.md + docs/exp26-pretraining-readiness.md). **12k looked like an ACCEPT (+0.041 lesion, CI excluded 0) but it was a FALSE POSITIVE** — the anatomy arm converges faster, so it only *looked* ahead at the intermediate. At the pre-registered 24k horizon (report40, n=40): lesion +0.023 but **CI [−0.007,+0.057] includes 0** (14/13/13 win/loss/tie), and pancreas **−0.014 CI excludes 0** (small significant regression). DO NOT ACCEPT. Anatomy shifts the operating point toward SPECIFICITY (38%→48%) but is not an accuracy lever and costs a little pancreas. Lesson banked: don't report intermediate reads; pre-registration + train-to-convergence caught the over-claim. Checkpoints archived (exp26A_lam0__…182106 / exp26B_lam03__…224437); per-case CSVs saved. **New infra from EXP-26 (reusable): anatomy5 label-mode + hbt_union resolver (fixes corrupt combined masks, 0% H/B/T overlap verified), AnatomyAwareLoss, collapse-aware eval, atomic-save + identity-verified strict resume + deterministic step-indexed data + --stop-after-step, frozen-cohort tooling, scripts/legacy/ cleanup, docs/interview-prep.md.**

**Model choice unchanged by EXP-26:** the plain whole-box SegResNet+SuPreM (EXP-24 clean, lesion 0.415 provided-ROI) remains the best base. Real levers stay: DATA SCALE + whole-box. Open Week-4 items still valid (more-healthy-data for specificity, clean localizer retrain for the autonomous number, mask-resolver-in-code — now partly delivered via hbt_union, EXP-23 containment). Anatomy = capstone (multi-structure) or a specificity dial, not the headline lever.

**M4A1 deliverable (Tuning/Orchestration/Deployment, due Sun 2026-07-27).** FINAL held-out TEST eval done (901 official test): lesion Dice **0.474 [95% CI 0.42–0.52]**, pancreas 0.827, detection **96%**, specificity **17%** — the honest headline, near the ~0.53 SOTA. Failure mode: over-segments small tumors 25–50× (small<1cm³ Dice 0.067) → caps Dice AND drives low specificity (one problem). Optuna (Bayesian TPE) HP search ran + logged to MLflow (`pants-level45-optuna`); confirmed LR near-optimal. Final model REGISTERED in MLflow: `pancreas-lesion-segmenter` v1 (step 18000). Deployment code (Codex-approved, 21 tests): `src/inference/predict.py`, `scripts/serve.py` (FastAPI `/predict`), `scripts/register_model.py`, `scripts/make_test_cohorts.py`. Report in `deliverables/week4/tuning-orchestration-report.md` (+ pipeline + by-size/phase diagrams in `deliverables/week4/img/`); checklist in `deliverables/week4/M4A1-checklist.md`. **Static-dataset fit-mapping documented** (real-time/DAG framing → justified manual pipeline). Remaining for Sunday: MLflow screenshots, endpoint smoke-test sample call/response, 2–3 sample overlays, git merge. **EXP-25 (fullhealthy) = REJECTED on the detection floor (2026-07-25, frozen official test 151 pos / 750 neg).** The specificity lever WORKED — spec 17%→**46%** (cleared the 30%+ bar decisively) — but detection fell 96%→**88%** (133/151), missing the pre-registered ≥90% floor by 3 cases, so by the rule set before results this is a reject (no goalpost-moving). Cost landed almost entirely on SMALL tumors: small<1cm³ detection 56% / Dice 0.013 (model stopped drawing them); lesion Dice all-pos 0.474→0.374. **Headline stays EXP-24 (0.474 / 96% det / 17% spec).** Valuable reject: proves low specificity is a movable DATA property, not a fixed flaw, and locates the cost. Next: pre-registered 1:3 fallback + low-threshold sweep (thr 0.30 already holds 40% spec) to find an operating point that may beat baseline on both axes — a Week-5 exploration. Full entry + table in `docs/experiments.md` (EXP-25 Result).

**WEEK 4 IN PROGRESS — autonomous, trustworthy pipeline (acting on Week 3 check-in feedback that provided-ROI numbers aren't comparable).** Built the localize-then-segment cascade (`scripts/cascade_eval.py`): a localizer finds the pancreas on the FULL CT, its predicted box (never GT) feeds the segmenter → removes the oracle at inference.

**⚠ CRITICAL — VALIDATION LEAKAGE FOUND (Codex audit 2026-07-19, VERIFIED, `docs/codex-audit-week4.md`). ALL scaled headline numbers are CONTAMINATED and withdrawn until a clean rerun.** `make_scaled_split.py` sampled from the manifest `split=="train"` column (the ImageTr *source folder*), NOT the carved `train.txt` fold, so scaled300/600/scaledmax overlap `val.txt` by 54/109/266 cases. Verified: **30/40 positive + 5/40 negative headline-eval cases were in `scaledmax` training.** So EXP-17/17c/20/22 numbers (0.528 / 0.483 / 90% / 94.6% / 98.9%) are NOT held-out — the model trained on most of the eval cohort. Canonical `train.txt`∩`val.txt` = 0 (that part was fine); the bug is only in the derived scaled splits. My earlier "no train/val leakage" claim was TRUE for canonical splits, FALSE for scaled ones. CORRECTION: the localizer is NOT "pancreas-only / never sees a lesion" either — it is the standard 3-class model, only the pancreas channel is used at inference (no oracle at test time, but the "leak-free by construction" framing was wrong).

**WEEK 4 PRIORITY ORDER (Codex top-3, correctness before any tuning):**
1. Fix `make_scaled_split.py` to sample ONLY from `train.txt`; add a startup assert that derived-split ∩ val/test == 0; rebuild scaled300/600/scaledmax (+ _clean); invalidate/delete their disk caches; RETRAIN both segmenter + localizer; rerun ALL headline evals on the disjoint cohort. Nothing is higher value.
2. One shared, config-driven pancreas resolver: union(combined, head/body/tail) with affine checks + drop combined when >300 mL. Requires CODE changes in `build_records` + `ComposeLabeld` (editing YAML `source_masks` alone does NOTHING — verified). Then rebuild clean splits from the RESOLVED mask.
3. Measure EFFECTIVE Stage-2 containment (after the 128³ center-crop, not the pre-resize box — current 94.6% is optimistic), with correct geometry, + an abstention/OOD fail-safe. Then EXP-23 resize-to-fit vs larger/aspect-preserving cube.
Also still open (Codex H4/H5/M4): harden `--resume` (still unsafe); cache tag omits HU/orientation/mask-policy → stale-cache risk after the mask/resize changes; several config keys advertised but not wired.
- Prior KEY FINDINGS still valid: the 0-voxel masks are genuine source defects (not a MONAI bug), 101 both-empty are one healthy cohort (site CH/SIEMENS), 9 subregion-recoverable, 7 corrupt-huge combined; containment clips + 27.5% oversize = 128³@1.5mm cube too small.

**LEAKAGE FIXED + REAL NUMBERS IN (2026-07-19).** `make_scaled_split.py` now samples from `train.txt` (+ disjointness assert); `train.py` has a startup LEAKAGE ABORT guard. Rebuilt `scaledmax_clean` (706+706, verified ∩val=0). Retrained the segmenter clean (`transfer_wholebox_scaledmax_CLEAN`, MLflow+archive). **HONEST provided-ROI numbers (val n=40, held-out): lesion Dice 0.415 raw / 0.412 cleaned, pancreas 0.817, specificity 15%/18%, detection 95% (38/40).** Vs contaminated 0.528 → the leak inflated lesion ~0.11 AND specificity ~2x. Number is REAL and solid (~0.53 reference), didn't collapse → the model generalizes.
- **KEY DIAGNOSIS (analyze_cases):** detection 95% is strong, but the model OVER-SEGMENTS tumors 3-13x (small-tumor Dice 0.11, medium 0.43, large 0.60; phase venous 0.58 > arterial 0.46 > non-contrast 0.25). Over-painting caps Dice AND drives the 15% specificity. This over-seg is the SAME one EXP-17c appeared to "fix" — that was partly leakage; on clean data it persists.

**TONIGHT'S RUN = EXP-18 Tversky loss** (wired: `train.py --loss tversky --tversky-alpha 0.7 --tversky-beta 0.3`). Penalizes false positives → should lift Dice AND specificity together. Single variable vs the 0.415 DiceFocal baseline. `tversky_focal` also wired (Tversky+focal) for a follow-up. Pre-registered EXP-18. SMOKE-TEST first.

**EXP-18 UPDATE (2026-07-19):** pure `tversky` alpha 0.7 REJECTED — pancreas collapsed to 0.000 (global FP penalty made the large organ not worth predicting). EXP-18b = `tversky_focal` alpha 0.6 (focal term protects pancreas): pancreas recovered to 0.833, lesion in-loop ~0.34 at step 16k (below the 0.415 DiceFocal baseline — expected if it's cutting over-segmentation; the SPECIFICITY at full eval decides win/loss). Run interrupted for transport; RESUME NOW HARDENED (see below) — finish from `runs/transfer_wholebox_clean_tverskyfocal__<STAMP>/last.pt`.

**`--resume` HARDENED (2026-07-19, fixes Codex H4):** `train.py` now restores `best_val` on resume (prefers the sibling `best.pt`'s recorded value) so a resumed run cannot clobber a good `best.pt` with a worse post-resume score; loop already continues from the checkpoint step; per-run archive keeps each run's best immutable. UNTESTED end-to-end (no MPS in the code-review sandbox) — verify the resume startup line prints the right step + `best_val restored to ...`. Optimizer/scheduler reload has a graceful fresh-optimizer fallback.

**Week-4 queue after EXP-18b:** more-healthy-data (specificity, we used only 706/6494 train-healthy); clean LOCALIZER retrain on scaledmax_clean (for the autonomous number); mask-resolver union(combined,head/body/tail)+>300mL guard IN CODE (build_records+ComposeLabeld, YAML alone does nothing); effective Stage-2 containment + EXP-23 resize-to-fit; drive preflight. All pre-registered in `docs/experiments.md`. Codex audit report = `docs/codex-audit-week4.md`.
